const ratingContainer = document.querySelector('.rating');
const ratingForm = document.querySelector('.rating__form');
const ratingInput = document.querySelectorAll('input[name="rating"]');
const ratingUserRatingContainer = document.querySelector(
  '.rating__user-rating'
);

export { ratingContainer, ratingForm, ratingInput, ratingUserRatingContainer };
